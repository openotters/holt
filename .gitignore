# build artifacts
/bin/
/dist/
coverage.out
# `go build` at the root drops the binary here, named after the module
/holt

# web console
cmd/holt/ui/node_modules/
cmd/holt/ui/dist/
cmd/holt/ui/tsconfig.tsbuildinfo

# embedded console build: rebuilt by `task ui:build` / goreleaser, only
# the .gitkeep placeholder is tracked (go:embed needs the dir to exist).
cmd/holt/internal/webui/dist/*
!cmd/holt/internal/webui/dist/.gitkeep

# local env
.env
.env.local

# task runner cache
.task/
