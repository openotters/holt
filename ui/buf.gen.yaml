version: v2
plugins:
  # Message types + service descriptors.
  - remote: buf.build/bufbuild/es
    out: src/gen
    opt: [target=ts]
  # connect-query React hooks (useQuery / useMutation per RPC).
  - remote: buf.build/connectrpc/query-es
    out: src/gen
    opt: [target=ts]
